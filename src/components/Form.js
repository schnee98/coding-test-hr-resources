const NAME_FIELD = `
  <div>
    <span class="form_description">이름</span>
    <span class="form_required">(필수*)</span>
  </div>
  <input id="name" placeholder="이름" />
`;

const EMAIL_FIELD = `
  <div>
    <span class="form_description">이메일</span>
    <span class="form_required">(필수*)</span>
  </div>
  <input id="name" placeholder="이메일" />
`;

const NICKNAME_FILED = `
  <div>
    <span class="form_description">닉네임</span>
    <span class="form_required">(필수*)</span>
  </div>
  <input id="name" placeholder="닉네임" />
`;

const ROLE_FIELD = `
  <div>
    <span class="form_description">직군</span>
    <span class="form_required">(필수*)</span>
  </div>
  <input id="name" placeholder="이름" />
`;

const MBTI_FIELD = `
  <div>
    <span class="form_description">MBTI</span>
  </div>
  <input id="name" placeholder="MBTI" />
`;

const FIELD_TYPES = {
  
}

export default function Form({ }) {
  
}
